{
  "commands": [
    {
      "name": "Calculator",
      "key": "calc",
      "commandPrefix": ">calc ",
      "fontIcon": "calculate",
      "subtitle": "Do simple math equations (powered by Qalc)",
      "processCommand": ["qalc", "-m", "100"],
      "hintIcon": "function",
      "hintText": "Open in calculator",
      "placeholderText": "Type an expression to calculate",
      "openCommand": "alacritty -e qalc -i '{input}'",
      "activateCommand": "qalc -t -m 100 '{input}'"
    },
    {
      "name": "Base64 Encode",
      "key": "base64",
      "commandPrefix": ">base64 ",
      "fontIcon": "code",
      "subtitle": "Encode text in base64",
      "processCommand": ["sh", "-c", "echo -n \"$0\" | base64"],
      "hintIcon": "content_copy",
      "hintText": "Copy to clipboard",
      "placeholderText": "Type text to encode in base64",
      "openCommand": "",
      "activateCommand": "echo -n '{input}' | base64"
    },
    {
      "name": "Bash Command",
      "key": ">",
      "commandPrefix": ">> ",
      "fontIcon": "terminal",
      "subtitle": "Execute bash command and copy output",
      "processCommand": ["sh", "-c", "eval \"$0\" 2>&1 | head -n 10"],
      "hintIcon": "terminal",
      "hintText": "Open in terminal",
      "placeholderText": "Type a bash command to execute",
      "openCommand": "alacritty -e sh -c '{input}; read -p \"\n\n...Press enter to close...\"'",
      "activateCommand": "{input}"
    }
  ]
}
